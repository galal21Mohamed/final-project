apiVersion: v1
kind: Service
metadata:
  name: shopverse-mysql
  namespace: shopverse
  labels:
    app: shopverse
    component: database
spec:
  selector:
    app: shopverse
    component: database
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  clusterIP: None
