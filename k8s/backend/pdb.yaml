apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: shopverse-backend-pdb
  namespace: shopverse
  labels:
    app: shopverse
    component: backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: shopverse
      component: backend
