apiVersion: v1
kind: Service
metadata:
  name: shopverse-frontend
  namespace: shopverse
  labels:
    app: shopverse
    component: frontend
spec:
  selector:
    app: shopverse
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
